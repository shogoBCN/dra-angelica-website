# Local emulator ONLY — copy to .env.local (never .env, never commit).
# Production uses Firebase Secret Manager (RESEND_API_KEY, CONTACT_TO_EMAIL, CONTACT_FROM_EMAIL).
# Do NOT create .env here: deploy fails if names overlap with secrets.
RESEND_API_KEY=re_xxxxxxxx
CONTACT_TO_EMAIL=doc.angelica@medicina-familiar.co
CONTACT_FROM_EMAIL=consultorio@medicina-familiar.co
